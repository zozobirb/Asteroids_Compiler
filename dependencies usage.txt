
Make sure too, when in a new shell:
export API_KEY=actual_key

Execute this command when changing grammar:
rm -rf gen bin
mkdir bin

Command for when updating grammar:
antlr4 -visitor -no-listener -o gen Glib_Gloob.g4

Execute this command when updating any java files:
javac -d bin -cp ".:lib/*" $(find gen src -name "*.java")
java -cp "bin:lib/*" Driver
<ctrl-d>
Btw grammar moved to project root, no longer in src!!


